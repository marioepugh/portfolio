// 'use strict';
// var app = app || {};
//
// (function(module) {
//   const repos = {};
//
//   repos.all = [];
//
//   repos.requestRepos = function(callback) {
// $.get(‘/github/user/repos’)
// .then(data => data.forEach(function(ele) {repos.all.push(ele)}),
// err => console.error(err))
// .then(callback);
// };
//
//   repos.with = attr => repos.all.filter(repo => repo[attr]);
//
//   module.repos = repos;
// })(app);
